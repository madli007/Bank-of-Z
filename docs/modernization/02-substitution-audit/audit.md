| legacy element | rediscovery ref | verdict | reason | trade-off |
|---|---|---|---|---|
| CICS subsystem - BANKZ core DB2 tables (ACCOUNT, CUSTOMER, PROCTRAN, CONTROL) | docs/modernization/01-rediscovery/integrations.md:18 | keep-as-is | Rediscovery marks these tables as load-bearing and directly used by core CICS business programs. They represent the current system of record for online banking operations. | Accept continued dependency on existing relational schema constraints and z/OS DB2 operational model. |
| CICS subsystem - Named Counter Service for account/customer number generation | docs/modernization/01-rediscovery/integrations.md:82 | replace-with-platform | Number generation is currently tied to CICS-specific named counter primitives and explicit ENQ/DEQ orchestration. A platform primitive can provide durable monotonic sequence management without custom transaction code. | Sequence migration must preserve existing numbering continuity and may change strict counter contention behavior under peak load. |
| CICS subsystem - ABNDPROC plus ABNDFILE write-only VSAM abend log | docs/modernization/01-rediscovery/integrations.md:69 | replace-with-platform | Rediscovery shows a single-writer, write-only abend sink used by all business programs. This is operational telemetry rather than domain logic and is better handled by platform logging/observability primitives. | Existing abend record shape and operator workflows will need remapping, and historical continuity may require backfill/adapters. |
| CICS subsystem - CRDTAGY1-CRDTAGY5 simulated credit-agency stubs | docs/modernization/01-rediscovery/repo-map.md:39 | retire | Rediscovery classifies these as stubs that simulate delay and random score generation, so they are not true business capabilities. They should not survive as production dependencies. | Removing them eliminates deterministic demo behavior used in test/demo flows; substitute test fixtures will still be needed for non-production scenarios. |
| CICS subsystem - Async child-transaction credit-check fan-out in CRECUST (RUN TRANSID OCR1-OCR5) | docs/modernization/01-rediscovery/integrations.md:131 | replace-with-platform | The orchestration pattern is implementation plumbing for parallel credit checks, not a business rule itself. A platform async execution primitive can replace custom channel/container fan-out and child-fetch mechanics. | Correlation, timeout, and failure semantics can shift during migration; preserving current response timing expectations may require additional controls. |
| CICS subsystem - Hard-coded sort-code constant (987654) and utility exposure | docs/modernization/01-rediscovery/repo-map.md:96 | replace-with-platform | Rediscovery highlights a hard-coded institution value embedded in copybooks/utilities. This is configuration data and should be externalized to managed runtime configuration. | Introduces runtime configuration dependency and potential misconfiguration risk that is currently impossible with compiled constants. |
| CICS subsystem - BNK1* BMS terminal screen-driver layer | docs/modernization/01-rediscovery/repo-map.md:15 | retire | Rediscovery already documents CICS business capabilities exposed through REST routes, while BNK1* programs are presentation-layer screen drivers. The 3270/BMS UI layer is legacy interaction surface, not core domain processing. | Operators/users relying on green-screen interaction lose that interface and will require runbook and access-path migration. |
| CICS subsystem - Hand-rolled DB2 deadlock retry loop in transfer logic | docs/modernization/01-rediscovery/business-rules.md:43 | replace-with-library | Transfer handling implements custom rollback, wait, and retry logic keyed to specific DB2 deadlock diagnostics. This is resilience plumbing that can be delegated to maintained retry/backoff libraries. | Standardized retry policies may not exactly match current retry count, delay cadence, and DB2 reason-code specificity without tuning. |
| CICS subsystem - GETCOMPY static company-name utility program | docs/modernization/01-rediscovery/repo-map.md:36 | retire | The program returns a static bank/company label via COMMAREA and does not encode domain behavior. This is metadata that should not persist as dedicated transaction logic. | Removing it can break callers that rely on the existing COMMAREA contract, so compatibility handling is needed during transition. |
| IMS subsystem - COBOL to Java history-write bridge via custom JNI declarations | docs/modernization/01-rediscovery/repo-map.md:229 | replace-with-library | Rediscovery flags a bespoke JNI bridge inside IMS for DB2 history writes, which is hand-crafted interoperability plumbing. Maintained interop/data-access libraries can replace low-level JNI ceremony. | Migration can change error propagation, transaction-boundary behavior, and performance characteristics compared with direct ByteBuffer/JNI control. |
| Batch subsystem - BNKSTMT hard-coded sort-code fallback (`123456`) | docs/modernization/01-rediscovery/integrations.md:169 | replace-with-platform | The statement job uses an embedded fallback sort-code when control input is absent, which is operational configuration rather than business capability. A platform configuration primitive is a better fit than compiled defaults. | Runtime config dependencies introduce new failure modes and environment-drift risk that do not exist with immutable in-program defaults. |
| API subsystem - OAuth2 endpoints using placeholder `example.com` authority | docs/modernization/01-rediscovery/integrations.md:276 | replace-with-platform | Rediscovery marks the published OAuth2 authority as a placeholder domain rather than a live identity integration. Authentication and token issuance should be provided by a platform identity primitive instead of static placeholder wiring. | Moving to platform identity changes token formats, claim sets, and operational ownership boundaries that existing clients may implicitly depend on. |
| Build/deployment integration - IBM MQ dataset libraries referenced but no MQ calls in source | docs/modernization/01-rediscovery/integrations.md:346 | retire | Rediscovery records MQ libraries as referenced while finding no `MQOPEN`/`MQGET`/`MQPUT` usage in COBOL or PL/I programs. This indicates an inactive or template-only integration surface, not load-bearing behavior. | If an external team relies on out-of-repo MQ exits or stubs, retiring this declared dependency could surface hidden coupling during cutover. |
| Batch subsystem - BNKSTMT executed manually with no scheduler artefacts in repo | docs/modernization/01-rediscovery/integrations.md:371 | replace-with-platform | Rediscovery shows statement generation is manually submitted or externally triggered, with no first-class scheduling integration captured in source control. A platform scheduling primitive is a better fit than manual operational submission. | Centralized scheduling introduces governance and dependency ordering constraints that can alter current ad-hoc operator timing and recovery practices. |
| Batch subsystem - Sequential control-card inputs (`DATECARD`, `SORTCODE`) for BNKSTMT | docs/modernization/01-rediscovery/integrations.md:168 | replace-with-platform | The batch job depends on fixed-format dataset inputs for run parameters, which is a dated file-drop style integration. Runtime parameters should be supplied through managed platform configuration/execution inputs instead of control-card files. | Replacing control cards can break existing JCL/operator procedures and requires strict validation to preserve defaulting semantics and month boundary behavior. |
| IMS subsystem - Flat-file load interfaces (`ACCTIN`, `CUSAIN`, `CUSTIN`, `HISTIN`, `TSTAIN`) | docs/modernization/01-rediscovery/integrations.md:176 | retire | Rediscovery classifies these file-driven loaders as load/init utilities outside the online transaction flow, making them legacy migration scaffolding rather than enduring integration contracts. | Retiring them removes a convenient bulk-seed path for non-production refreshes, so alternate data-seeding procedures are still required. |
| CICS subsystem - Relationship integrity enforced only by application logic (no DB2 foreign keys) | docs/modernization/01-rediscovery/data-model.md:134 | replace-with-platform | Rediscovery records customer-account and account-transaction links as convention-based with no DB2 FK enforcement. This data-store integrity concern should be enforced by platform-managed relational constraints rather than only by program behavior. | Adding enforced constraints can reject currently tolerated orphan or out-of-order writes, requiring cleanup and controlled migration sequencing. |
| CICS subsystem - Schema drift between deployed DDL and SQL DECLARE copybooks | docs/modernization/01-rediscovery/data-model.md:211 | replace-with-platform | Rediscovery flags non-identical column definitions (precision and date representation) across schema artifacts, which creates ambiguity in the authoritative data model. Canonical platform schema governance should replace divergent definitions. | Convergence may require data conversion and stricter validation that can expose previously hidden truncation/format issues in live records. |
| CICS subsystem - CONTROL table qualifier mismatch (`STTESTER.CONTROL` vs `BANKZ.CONTROL`) | docs/modernization/01-rediscovery/data-model.md:212 | replace-with-platform | The rediscovery notes a schema-name mismatch between copybook SQL declarations and deployment DDL, indicating environment-coupled datastore bindings. Platform-managed environment/schema binding should replace hard-coded divergent qualifiers. | Standardizing schema binding can break existing bind/package assumptions and needs coordinated rollout across compile/bind pipelines. |
| CICS subsystem - Persisted `ACCOUNT_OVERDRAFT_LIMIT` not used by transaction paths | docs/modernization/01-rediscovery/open-questions.md:31 | retire | Rediscovery records that overdraft-limit data is stored but not consumed by debit/transfer logic, indicating dead or orphaned data shape in the operational store. The unused field should be retired unless a verified external dependency exists. | If an out-of-repo consumer still reads this column, retiring it can cause downstream reporting or control regressions until those consumers are remediated. |
| CICS subsystem - `BANKZ.CONTROL.CONTROL_VALUE_STR` unused optional payload column | docs/modernization/01-rediscovery/open-questions.md:37 | retire | Rediscovery identifies this column as present in DDL but not read or written by in-repo programs, making it a likely dead schema artifact. It should not survive modernization without proven runtime usage. | Removing it reduces schema flexibility for ad hoc metadata and may require a future extension path if latent use-cases reappear. |
| IMS subsystem - HDAM/OSAM hierarchical datastores for online flows | docs/modernization/01-rediscovery/integrations.md:43 | replace-with-platform | Rediscovery states all IMS databases use HDAM/OSAM access, which reflects a legacy hierarchical storage model tightly coupled to program access paths. A platform data primitive better aligned to modern access and operability should replace this storage assumption. | Migrating hierarchical segments to a platform store can change access-path performance and requires careful transformation of segment-key semantics. |
| IMS subsystem - DBD inventories with no corresponding online program usage (`ACCTYPE`, `CUSTTYPE`, `TSTAT`, `TSTATTYP`, `TTYPE`) | docs/modernization/01-rediscovery/open-questions.md:25 | retire | Rediscovery flags five IMS databases with no in-repo online consumers, indicating likely dead or externally orphaned data structures. These datastores should be retired unless proven as live dependencies. | Retirement risks breaking hidden external jobs if usage exists outside the repository; dependency confirmation is required before final removal. |
| API subsystem - z/OS Connect runtime pinned to `zosConnect-3.0` feature level | docs/modernization/01-rediscovery/integrations.md:196 | replace-with-platform | The API layer is tied to a specific middleware runtime level, creating version-lifecycle coupling in the delivery stack. Replacing runtime-specific hosting assumptions with platform-managed API hosting reduces framework lifecycle exposure. | Platform migration can change request/response mediation behavior and operational control points currently used in the existing runtime. |
| Frontend subsystem - Carbon Web Components loaded from versioned CDN URL (`v2.47.0`) | docs/modernization/01-rediscovery/integrations.md:290 | replace-with-library | UI rendering depends on a remotely hosted, version-pinned framework asset path, which introduces runtime and lifecycle risk outside release control. Managed library dependency handling is preferable to direct CDN runtime coupling. | Packaging components with the app increases artifact size and shifts responsibility for patch cadence and cache strategy to the delivery team. |
| IMS subsystem - Java bridge dependency pin (`com.ibm.db2:jcc:12.1.4.0`) in Gradle build | docs/modernization/01-rediscovery/integrations.md:297 | replace-with-library | The IMS Java bridge is coupled to an explicitly pinned JDBC driver version, creating manual upgrade and compatibility burden over time. Library-level dependency governance should replace ad hoc version pinning in a hybrid runtime path. | Driver upgrades can alter SQL behavior, TLS defaults, and performance characteristics, requiring regression validation against HISTORY read/write flows. |
| IMS subsystem - COBOL and PL/I login implementations coexisting (`IBLOGIN1.cbl` and `IBLOGIN.pli`) | docs/modernization/01-rediscovery/open-questions.md:8 | retire | Rediscovery marks the PL/I login as a rewrite while both language variants still exist, indicating duplicate runtime paths and dead-code risk. One superseded implementation should be retired after production-path confirmation. | Premature retirement can remove a fallback transaction path if operations still route traffic to the older implementation in some environments. |
| Local tooling subsystem - containerized local tooling declared as non-runtime (`docker-compose.yaml`) | docs/modernization/01-rediscovery/repo-map.md:244 | keep-as-is | Rediscovery explicitly classifies these containers as local tooling and not z/OS runtime dependencies, so they do not represent production runtime EOL exposure. Keeping them unchanged preserves developer bootstrap consistency. | Retains dependence on local container engine availability for onboarding and troubleshooting workflows. |
| Build/deployment subsystem - DB2 subsystem identifier hard-coded as `DBD1` in JCL | docs/modernization/01-rediscovery/integrations.md:13 | replace-with-platform | Rediscovery shows environment binding is encoded directly in JCL for DB2 subsystem selection, creating portability friction across environments. Runtime platform configuration should own environment targeting rather than static job text. | Externalizing subsystem binding introduces deployment-time configuration controls that can be mis-set and require stronger promotion governance. |
| IMS subsystem - Fixed IMS dataset naming assumptions (`BANKZ.IMS2`, `RECON1-3`) | docs/modernization/01-rediscovery/integrations.md:60 | replace-with-platform | Rediscovery captures hard-bound IMS HLQ and RECON dataset naming conventions in configuration, tying operations to specific storage topology assumptions. Platform-managed data-service provisioning should replace static dataset identity coupling. | Decoupling from fixed dataset names requires migration tooling and can disrupt established operator diagnostics keyed to current dataset identifiers. |
| API subsystem - Static localhost defaults for backend connections (`localhost:4320`, `localhost:9977`) | docs/modernization/01-rediscovery/integrations.md:209 | replace-with-platform | Rediscovery records baked-in host/port defaults for CICS and IMS connections, reflecting static endpoint assumptions instead of dynamic service binding. Platform service discovery and managed endpoint configuration are better operational fits. | Moving to dynamic endpoint resolution adds control-plane dependencies and can complicate troubleshooting when endpoint resolution fails. |
| Frontend subsystem - API proxy fallback to `http://localhost:9080` | docs/modernization/01-rediscovery/integrations.md:285 | replace-with-platform | Rediscovery shows the frontend server can fall back to a local API endpoint, which is a local-network operational assumption unsuitable for portable runtime deployment. Endpoint routing should be provided by platform ingress/configuration primitives. | Replacing fallback behavior removes a convenient local default and can slow developer bring-up unless equivalent dev configuration is provided. |
| Frontend subsystem - Hard-coded runtime sort-code default (`987654`) in client config | docs/modernization/01-rediscovery/integrations.md:288 | replace-with-platform | Rediscovery identifies business environment data embedded directly in frontend configuration, coupling runtime behavior to a static value. Platform-managed configuration should supply institution-specific runtime values instead of compiled defaults. | Centralized configuration adds dependency on configuration delivery and creates new failure modes if values are missing or stale. |

# Open Questions

> Questions that cannot be answered from source code alone. Require domain expert or production system access.

| # | Question | Evidence | Impact |
|---|----------|----------|--------|
| 1 | `UPDCUST.cbl` header says it accesses "the VSAM datastore" but the CICS setup JCL only creates DB2 tables; no VSAM KSDS DDL or DEFINE CLUSTER is present in the repo. Is customer data stored in VSAM, DB2, or both? | `src/base/cics/cobol/UPDCUST.cbl:14`; `.setup/jcl/cics/Db2-create.jcl` | Critical for data-model mapping |
| 2 | `IBLOGIN.pli` is described as a rewrite of `IBLOGIN1.cbl`. Which one is active in production — the COBOL or the PL/I version? Are both deployed under different transaction codes? | `src/base/ims/pli/IBLOGIN.pli:3`; `src/base/ims/cobol/IBLOGIN1.cbl:4` | Dead-code risk if one is superseded |
| 3 | No automated test suite was found. Do unit tests, integration tests, or z/OS batch test jobs exist outside this repository (e.g., in a separate test dataset or private repo)? | Entire repo search | Affects modernisation confidence |
| 4 | `src/base/cics/copy/WAZI.cpy` is an empty copybook with the comment "THIS IS JCs EDIT". Is this intentionally empty, a placeholder for future content, or dead code that should be removed? | `src/base/cics/copy/WAZI.cpy:8` | Minor — cleanup vs. intentional placeholder |
| 5 | `ABNDPROC.cbl` writes abend records to a "centralised CF (KSDS) datastore". What is the dataset name, and is this still operational in production? | `src/base/cics/cobol/ABNDPROC.cbl:11` | Infrastructure dependency |
| 6 | The sort-code is hard-coded as `987654` in `SORTCODE.cpy`. Is this value the same in production, or is it overridden at deploy time (e.g., via CICS resource definition or JCL symbolic)? | `src/base/cics/copy/SORTCODE.cpy:7` | Hard-coded value is a migration risk |
| 7 | `BANKDATA.cbl` is in `src/base/cics/cobol/` but has no `CBL CICS(...)` directive — it appears to be a standalone DB2 batch initialiser. Is it run as a CICS program, a batch job, or a utility? Does it have accompanying JCL? | `src/base/cics/cobol/BANKDATA.cbl:1` | Classification affects deployment scope |
| 8 | `IBTRAN.cbl` calls Java class `nazare.jmp.controller.InsertHist` via JNI. What JVM configuration is used in the IMS region, and is there a known latency or failure mode for this cross-language call? | `src/base/ims/cobol/IBTRAN.cbl:20`; `src/base/ims/copy/JNI.cpy` | Risk area for re-platforming |
| 9 | Five credit-agency stubs (`CRDTAGY1`–`CRDTAGY5`) simulate random credit scores. In production, are these replaced by real external credit-agency integrations, or are the stubs the production code? | `src/base/cics/cobol/CRDTAGY1.cbl:8` | Determines whether an external API contract exists |
| 10 | The CICS CONTROL table (`BANKZ.CONTROL`) is used for named counters (customer number, account number). What is the current maximum customer/account number in production, and is there any near-overflow concern? | `src/base/cics/copy/CONTROLI.cpy`; `src/base/cics/copy/CONTDB2.cpy` | Data-capacity planning |
| 11 | In `XFRFUN.cbl`, `SQLERRD(3)=13172872` is treated as deadlock in one branch and as timeout text in another branch, while `13172894` is also labeled timeout. Which `SQLERRD(3)` values are the intended canonical mappings for deadlock vs timeout in production DB2 diagnostics? | `src/base/cics/cobol/XFRFUN.cbl:1199`; `src/base/cics/cobol/XFRFUN.cbl:1288`; `src/base/cics/cobol/XFRFUN.cbl:1485`; `src/base/cics/cobol/XFRFUN.cbl:1529` | Affects operational runbook interpretation and incident triage |
| 12 | `BANKZ.ACCOUNT` numeric precision differs between deployment DDL and SQL DECLARE copybook (`INTEREST_RATE` is `DECIMAL(6,2)` in DDL but `DECIMAL(4,2)` in `ACCDB2.cpy`; balances are `DECIMAL(12,2)` in DDL but `DECIMAL(10,2)` in `ACCDB2.cpy`). Which definition is authoritative in production? | `.setup/jcl/cics/Db2-create.jcl:31`; `.setup/jcl/cics/Db2-create.jcl:36`; `.setup/jcl/cics/Db2-create.jcl:37`; `src/base/cics/copy/ACCDB2.cpy:13`; `src/base/cics/copy/ACCDB2.cpy:18`; `src/base/cics/copy/ACCDB2.cpy:19` | Direct impact on data-type mapping and truncation risk |
| 13 | `PROCTRAN_DATE` is `DATE` in DDL but declared as `CHAR(8)` in `PROCDB2.cpy`. Is this intentional for precompiler compatibility, or is one of these definitions stale? | `.setup/jcl/cics/Db2-create.jcl:57`; `src/base/cics/copy/PROCDB2.cpy:12` | Affects date semantics and conversion rules |
| 14 | `CONTDB2.cpy` declares `STTESTER.CONTROL`, while deployment DDL creates `BANKZ.CONTROL`. Is `STTESTER` an environment-specific qualifier, or is the copybook outdated? | `src/base/cics/copy/CONTDB2.cpy:7`; `.setup/jcl/cics/Db2-create.jcl:70` | Schema-name mismatch can break static SQL binding |
| 15 | For CICS account type, code and seed data use `'SAVING'` (7 chars) while domain language usually uses "SAVINGS". Is `'SAVING'` the canonical stored code? | `src/base/cics/cobol/CREACC.cbl:1312`; `src/base/cics/cobol/BANKDATA.cbl:1364` | Needed for API/domain vocabulary alignment |
| 16 | `datasets.yaml` references IBM MQ libraries (`CSQ.V9R1M0.SCSQCOBC`, `CSQ.V9R3M0.SCSQPLIC`, etc.) but no `MQOPEN`/`MQGET`/`MQPUT` calls appear in any COBOL or PL/I source. Was MQ integration removed, is it planned, or is this a template carry-over? | `.setup/build/datasets.yaml:53–67` | Determines whether MQ is in scope for modernisation |
| 17 | The OAuth2 endpoints in the OpenAPI spec (`https://auth.bankofz.example.com/oauth/authorize` and `.../token`) use an `example.com` domain. Is there a real identity provider (e.g., IBM Security Verify, RACF, or third-party IdP) behind this, or is OAuth2 not implemented in production? | `src/api/src/main/api/openapi.yaml:627–628` | Affects API security posture and modernisation of auth layer |
| 18 | `BNKSTMT.pli` falls back to sort-code `'123456'` if the `SORTCODE` DD is absent, whereas the CICS subsystem hard-codes `987654` in `SORTCODE.cpy`. Are these two values intentionally different (different environments), or is the PL/I fallback a bug? | `src/base/batch/pli/BNKSTMT.pli:256`; `src/base/cics/copy/SORTCODE.cpy:7` | Data consistency — statements could be generated for the wrong sort-code |
| 19 | Five IMS DBDs (`ACCTYPE`, `CUSTTYPE`, `TSTAT`, `TSTATTYP`, `TTYPE`) have no corresponding online COBOL program that reads or writes them. Are these databases populated and queried by programs outside this repository, or are they dead artefacts? | `src/base/ims/DBD/ACCTYPE.asm`, `CUSTTYPE.asm`, `TSTAT.asm`, `TSTATTYP.asm`, `TTYPE.asm` | Dead-code risk; affects IMS database inventory |
| 20 | The `BNKSTMT` batch job has no scheduler JCL (TWS, OPC, or equivalent) in the repository. How is the monthly statement run triggered in production — manual submission, external scheduler, or cron on USS? | `src/base/batch/jcl/BNKSTMT.jcl` | Operational runbook gap |
